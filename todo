确定我改了 policy-web-viewer 的 JS 或 HTML，export 出来的 WASM 页面也会跟着变化吗

会。

现在本地 API demo 和静态 WASM export 共用同一份前端源码：

- 前端源码：frontend/
- 本地构建输出：src/policy_web_viewer/static/
- WASM 导出构建输出：policy-web-viewer-export 指定的 --out 目录

需要修改 UI 时，只改 frontend/ 下的 HTML、CSS、JS。不要手动编辑 src/policy_web_viewer/static/，它是 npm run build:api 生成的本地服务静态资源。
