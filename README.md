# policy-web-viewer browser simulation demo (g1)

This directory is a static browser runtime. It runs MuJoCo WASM and ONNX Runtime Web in the browser, so command sliders and drag forces affect the live simulation.

Deploy the contents of this directory to GitHub Pages. For local inspection, serve it with:

```bash
python3 -m http.server 8080
```
